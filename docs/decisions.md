# Decisions
## ADR-001 Extractive fallback
Accepted: local deterministic embeddings and extractive answers provide an offline baseline. Low-scoring retrieval must return insufficient evidence.

## Evaluation examples
The generated test PDF asks when inspection occurs and requires a page-1 citation; an empty hit set must refuse to answer.
