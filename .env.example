DATABASE_URL=sqlite:///./rag.db
# Compose only: set a URL-safe password locally; never commit the real value.
POSTGRES_PASSWORD=
QDRANT_PATH=./qdrant_data
UPLOAD_DIR=./uploads
EVIDENCE_THRESHOLD=0.18
